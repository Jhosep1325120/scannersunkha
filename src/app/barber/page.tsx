// Comentario: Carga la vista principal que usa el barbero para escanear clientes.
import BarberPageClient from '@/components/BarberPageClient'

export default function BarberPage() {
  return <BarberPageClient />
}
